# 검증 기록 — 2026-09-07

## 누적되는 주민마당·받은 자료함과 즉시 반응하는 추리 노트

- 공개 글 80개와 비공개 기록 32개가 중복 없이 전체 112개 기록을 구성한다. 공개 장부는 초기부터 읽을 수 있고, 오래된 비공개 원본과 미래 공개 글은 각각 회신과 게시 시각을 기다린다.
- 모든 단계에서 기존 공개 글이 남으며 이전 사건 재방문 시 세계의 시각과 게시글 목록이 되돌아가지 않는다. 서버도 동일한 접근 규칙을 적용한다.
- 전체 UI 캠페인은 공개 글을 주민마당에서, 비공개 원본을 받은 자료함에서 읽어 완료한다. 단계가 바뀌어도 동일한 게시판 DOM이 유지되는지 검사한다.
- 저장 응답을 의도적으로 보류한 상태에서 체크·해제·다른 질문 변경을 연속 실행했다. 즉시 표시·증거 개수 제한·모달 재열람·이전 응답에 의한 덮어쓰기 방지·대기 요청 합치기·최종 저장 뒤 검증을 확인한다.
- 이전 저장 요청과 마지막 저장 요청을 각각 실패시켜 최신 선택의 보존, 잘못된 검증 방지, 미저장 상태의 종료 방지와 명시적 재시도를 검증한다.

| 범위             | 검증                                                                                                   | 결과                |
| ---------------- | ------------------------------------------------------------------------------------------------------ | ------------------- |
| 8개 사건         | 독립 공략의 24개 가설, 기존 핵심 기록 48개, 힌트·오답·정답·두 결말                                     | 전체 통과           |
| 주민 게시판      | 일상 글 64개를 포함한 공개 글 80개, 별도 비공개 기록 32개, 분류 다양성, 댓글·날짜·사진 파일             | 통과                |
| 이미지 프롤로그  | 사건마다 장면 이미지·중심 의문·조사 이유, 개별 문제를 미리 노출하지 않음, 최초 표시·재열람·저장        | 통과                |
| 의문 발견        | 8화의 모든 의문에 관련 기록 연결, 읽기 전 숨김, 일상 글로 해금되지 않음, 이전 답안·해결 기록 유지      | 통과                |
| 사건의 연결      | 전출 안내 → 계정 동일성 → 마지막 약속 상대, 의문별 이유·목적 표시, 해당 추리 노트로 초점 이동          | jsdom 상호작용 통과 |
| 게시판 조작      | 12개씩 페이지, 검색 시 페이지 초기화, 사진 필터, 기존 글 유지, 게시 시각 전의 글 제외                  | 통과                |
| 증거·추리 모달   | 게시판 DOM·검색어·페이지 유지, 원문 열기, 같은 원문 다시 열기, 취소 이벤트, 중첩 창의 배경 스크롤 잠금 | jsdom 상호작용 통과 |
| 진행 호환성      | 새 필드 없는 저장 상태에서 메모 유지, 공감 토글, 프롤로그 중복 저장 방지, 초기화                       | 통과                |
| 근거 판정        | 일상 글로 정답 근거 대체 불가, 공개된 옛 장부 접근 허용, 미수신 원본·미래 게시글·잠긴 도입 접근 거부   | 통과                |
| 실제 저장소      | D1 HTTP 전체 캠페인, 공감·프롤로그·메모·답안·진행 재요청, 세션 격리                                    | 통과                |
| 오류 처리        | 로드·저장 실패 재시도, 잘못된 JSON·과다 크기, 다른 출처 요청 거부                                      | 통과                |
| 이미지·정적 파일 | 신규 WebP 14개, 공유 이미지, 한글 HTML, CSS·JS 제공                                                    | HTTP 통과           |
| 품질 검사        | TypeScript, ESLint, vinext 배포 빌드                                                                   | 통과                |

## 실행

- `npm test`: 15개 테스트. 세계의 누적과 자료 접근, 느리거나 실패한 저장, 의문 발견과 저장 호환성, React + jsdom 게시판/중첩 모달 동작, 8화 전체 풀이를 검증한다.
- `npm run typecheck`, `npm run lint`: 통과. 이미지는 사전 압축된 정적 WebP이므로 Next 이미지 서비스 권고는 앱 파일에서 제외했다.
- `npm run test:http`: 개발 서버에서 8화 전체와 실제 저장소 검증 통과. 개발 서버와 배포 산출물의 서로 다른 CSS/JS 경로를 모두 검사한다.
- `npm run build`: vinext 5단계 빌드 통과. 요청 헤더를 사용하는 메타데이터 때문에 경로 분류는 `?`지만 HTML 응답은 200이다.
- 빌드된 Worker를 로컬 포트 3001에서 실행하고 `GAME_TEST_URL=http://127.0.0.1:3001`에서 HTTP 캠페인을 검증했다. D1은 `.wrangler/production-test` 아래의 로컬 테스트 저장소를 사용했다.
- 최초 로컬 산출물 검사에서 이미지 요청 하나가 지연되었다. 로컬 서버 출력을 읽은 뒤 완료되었으며, 이후 직접 요청과 나머지 13개 이미지·CSS·JS는 모두 정상 응답했다. 최종 산출물 서버 로그는 파일에 보관한다.

## 확인 한계

인과관계 업데이트에서는 8화의 24개 의문이 실제 해당 사건 기록에서 발견되는지, 아직 읽지 않은 의문은 숨기는지, 기존 답안과 해결 기록은 계속 열리는지 검사했다. 첫 전출 안내로 전출 설명에 대한 의문만 생기고, 다른 관련 글을 읽은 뒤 계정·약속 상대에 대한 의문이 추가된다. 조사 이유와 전체 사건에의 기여, 특정 의문으로 이동할 때의 초점, 고정 해결 목표 창도 확인한다. 발견 상태는 기존 읽음·답안·해결 상태로 계산하며 저장 형식은 변경하지 않는다. 기존 답·근거의 준비/입증/재검토 상태 검사는 유지한다.

최종 HTTP 검증에서 다른 출처의 요청을 거부한 직후 같은 연결의 다음 요청이 로컬 Worker에서 503으로 중단되는 현상을 재현했다. 요청 본문을 소비한 뒤 출처 오류를 반환하도록 수정했다. 연속 오류 요청의 403 → 400 응답을 세 차례 확인하고 최종 배포 산출물에서 8화 전체·두 결말·저장·이미지·정적 자산 검증을 모두 통과했다.

이 환경의 in-app Browser에는 사용 가능한 브라우저가 없었다. 실제 브라우저 스크린샷·화면 배치·네이티브 모달 초점 및 터치 동작을 검증했다고 주장하지 않는다. UI 제어와 상태 보존은 jsdom에서, 배포 런타임과 데이터 저장은 실제 로컬 Worker/D1 HTTP 요청으로 확인했다. 로그인으로 보호되는 게시 주소의 인증 이후 화면은 사용자 브라우저에서 확인할 수 있다.
